package concurrency;

import java.util.concurrent.Callable;

public class CallableInt implements Callable<Integer>{

	String word;
	public CallableInt(String word) {
		this.word = word;
	}

	@Override
	public Integer call() throws Exception {
		return word.length();
	}

}
